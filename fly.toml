app = "signal-forwarder"
primary_region = "waw"

[build]

[env]

[experimental]
  auto_rollback = true

[processes]
  worker = "python main.py"

[checks.commands]
  command = "echo running"
  interval = "30s"
  timeout = "10s"